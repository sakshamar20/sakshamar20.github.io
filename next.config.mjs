/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/Saksham-Arora",
  assetPrefix: "/Saksham-Arora/",
  images: { unoptimized: true },
};

export default nextConfig;
